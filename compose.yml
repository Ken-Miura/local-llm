services:
  local-llm:
    image: ollama/ollama:0.13.5
    container_name: local-llm-container
    volumes:
      - local-llm:/root/.ollama
    ports:
      - "11434:11434"
    # Comment out this deploy section if you don't use any GPUs.
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]

  local-llm-webui:
    container_name: local-llm-webui-container
    image: ghcr.io/open-webui/open-webui:git-aaea9a5-cuda
    volumes:
      - local-llm-webui:/app/backend/data
    ports:
      - "8080:8080"
    environment:
      - 'WEBUI_AUTH=false'
      - 'ENABLE_OLLAMA_API=true'
      - 'OLLAMA_BASE_URL=http://local-llm-container:11434'
      - 'ENABLE_WEB_SEARCH=true'
      - 'WEB_SEARCH_ENGINE=duckduckgo'

volumes:
  local-llm:
  local-llm-webui:
